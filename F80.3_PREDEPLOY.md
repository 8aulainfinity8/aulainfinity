# FASE F80.3 — DIAGNÓSTICO PRE-DEPLOY

## 1. PROYECTO DETECTADO
- **GCP/Firebase Project:** `aulainfinity8-a6ac0` (Indicado por contexto, entorno local del workspace sin gcloud inicializado de forma persistente, pero definido).
- **Firebase config:** `firebase.json` contiene la integración correcta con `firestore.rules`, `storage.rules`, `functions` y los emuladores pertinentes.

## 2. ARCHIVOS QUE SERÁN DESPLEGADOS
Las modificaciones originadas en la fase F80.2 y confirmadas en el actual workspace:
- `firestore.rules`
- `functions/index.ts` (junto con el build resultante `functions/lib/index.js`)
- Client code compilado a través de Vite (`dist/` dir), específicamente las llamadas ajustadas en `src/services/firestoreSync.ts`.

## 3. FUNCIONES DETECTADAS (functions/index.ts)
Se confirman presentes y estructuradas correctamente las funciones:
- **`scheduledChatRetentionCleanup`**: Presente y activa. Emplea correctamente CRON (`0 2 * * *`), paginación `limit(500)` mediante `dateToCheck < threshold`, maneja la exploración de subcolecciones (`messages`, `signal`, `documents`), y utiliza un `bulkWriter` para suprimir la limitación tradicional de `batch()`.
- **`adminClearChatMessages`**: Presente como un `onCall`. Integra la validación del contexto de autenticación y de autorización (`isCallerAdmin` derivado de `context.auth.token.role === 'admin'`). También delega su borrado en `bulkWriter()` recorriendo colecciones hijas y heredadas.

## 4. RESULTADO DEL BUILD
- **Comando:** `npm run build`
- **Resultado:** APROBADO.
- **Detalle:** `✓ 3555 modules transformed. built in ~26-29s`. No hay errores de TS (Type Checking) ni fallas en la configuración de esbuild / Vite para Node.js o el browser build.

## 5. ESTADO DE FIRESTORE RULES
Las reglas de seguridad se revisaron profundamente:
- **Protección de `delete`:** Las colecciones *legacy* (`firestore_direct_messages`, `firestore_peer_messages`, `firestore_teacher_messages`, `firestore_course_messages`) han modificado sus reglas. Solo un Administrador (`isAdmin()`) o el autor original (`resource.data.senderId == request.auth.uid`) pueden eliminarlos. El bypass de `isParticipant` ya no aplica al permiso `delete`.
- **Protección de fechas futuras:** Los atributos temporales de la base (como `lastMessageTimestamp`) tienen checks de tope de tiempo real vía `request.time`.

## 6. POSIBLES RIESGOS
- **Storage Huérfano:** Como se observó en la F80.2, el borrado de conversaciones por `adminClearChatMessages` o la retención `scheduledChatRetentionCleanup` eliminan los documentos de Firestore pero no rastrean activamente bucket blobs subidos que pudieran estar asociados al chat. En el futuro puede ser necesario un Garbage Collector enfocado a Storage si esto afecta la cuota. 

## 7. VEREDICTO PRE-DEPLOY
PASS
